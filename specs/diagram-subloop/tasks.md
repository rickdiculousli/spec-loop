# Tasks — diagram-subloop

- [ ] Add the diagram-subloop clause to `skills/brainstorm/SKILL.md` (in step 2, alongside the existing "Use the project's decision tools" paragraph): when `specs/README.md` or `specs/HOUSE-RULES.md` declares a diagram tool, maintain a diagram of the design structure in `specs/<slug>/`; update only on structural change (new component, moved scope boundary, new dependency); keep a compact text source of truth; never hand-write raw canvas JSON (e.g. `.excalidraw` element arrays); no declared tool → no subloop. Validation: read-through confirms all four rules plus the no-op default, and no other step of the skill changed.
- [ ] Add a commented-out example diagram-tool declaration line to the Conventions section of `templates/specs-README.md`, matching the wording the brainstorm clause looks for. Validation: `grep "diagram" templates/specs-README.md` shows the example line.
- [ ] Document the subloop in `README.md`'s brainstorm section — one short paragraph: declared-tool trigger, structural-change gating, engine stays per-repo. Validation: `grep -i "diagram" README.md` hits in the brainstorm section and the wording matches the skill clause.
- [ ] Add an icebox entry for the full `/visualize` feature (bundled engine + standalone skill + compiler) to this repo's `specs/README.md`, pointing at `specs/diagram-subloop/design.md`, with re-entry criteria: the ecosystem consolidates on a maintained pip-or-single-binary engine with hand-edit preservation, or two consuming repos independently build the same per-repo setup. Validation: `grep "visualize" specs/README.md` shows the entry with both criteria.
- [ ] Bump `version` to `0.9.0` in `.claude-plugin/plugin.json`. Validation: `grep '"version"' .claude-plugin/plugin.json` shows `0.9.0`.
- [ ] Mark spec status `done`
