# Tasks — dry-run-command-verification

This spec is a stub (see `proposal.md`'s Open Questions section) — not yet ready for
`/implement`. No concrete tasks exist yet because the design itself isn't resolved: writing
specific file/command-level tasks now would mean inventing answers to open questions that
need real judgment (dry-run mechanism, which constructs are tractable, what "safe" means).

- [ ] Resolve the open questions in `proposal.md` (dry-run mechanism, construct scope,
  definition of "safe", relationship to `legible-bash.sh` and CLAUDE.md's dependency
  invariants) in a dedicated design session, then replace this file with a real, concrete
  task list and add a `## Success criteria` section with Current/Target/Acceptance triples
  to `proposal.md`.
